# DO NOT EDIT THIS FILE MANUALLY
#
# This file is auto-generated and any manual changes will be overwritten, nightly.
#
# - to change this file: modify .github/templates/test-new-repo.tmpl
# - to add a new distro: modify main.go and add a new entry to `supportedDistros`
# - to add new codename: create a subfolder in corresponding distro folder

name: Test New Repository

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  debian-bookworm:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/debian/${{ matrix.stage }} bookworm main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on debian bookworm
        uses: ./debian/bookworm
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  debian-bullseye:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/debian/${{ matrix.stage }} bullseye main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on debian bullseye
        uses: ./debian/bullseye
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  debian-testing:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/debian/${{ matrix.stage }} testing main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on debian testing
        uses: ./debian/testing
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-focal:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/ubuntu/${{ matrix.stage }} focal main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on ubuntu focal
        uses: ./ubuntu/focal
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-jammy:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/ubuntu/${{ matrix.stage }} jammy main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on ubuntu jammy
        uses: ./ubuntu/jammy
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-lunar:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/ubuntu/${{ matrix.stage }} lunar main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on ubuntu lunar
        uses: ./ubuntu/lunar
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-mantic:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/ubuntu/${{ matrix.stage }} mantic main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on ubuntu mantic
        uses: ./ubuntu/mantic
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-noble:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/ubuntu/${{ matrix.stage }} noble main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on ubuntu noble
        uses: ./ubuntu/noble
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-oracular:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, stable]
    env:
      apt-key-url: http://archive.regolith-desktop.com/regolith.key
      apt-repo-line: "deb [arch=amd64] http://archive.regolith-desktop.com/ubuntu/${{ matrix.stage }} oracular main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ env.target-package }} ${{ matrix.stage }} on ubuntu oracular
        uses: ./ubuntu/oracular
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}
