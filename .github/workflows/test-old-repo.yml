# DO NOT EDIT THIS FILE MANUALLY
#
# This file is auto-generated and any manual changes will be overwritten, nightly.
#
# - to change this file: modify .github/templates/test-old-repo.tmpl
# - to add a new distro: modify main.go and add a new entry to `supportedDistros`
# - to add new codename: create a subfolder in corresponding distro folder

name: Test Old Repository

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  debian-bookworm:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bookworm-amd64 bookworm main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on debian bookworm
        uses: ./debian/bookworm
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  debian-bullseye:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 bullseye main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on debian bullseye
        uses: ./debian/bullseye
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  debian-testing:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-testing-amd64 testing main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on debian testing
        uses: ./debian/testing
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-focal:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-ubuntu-focal-amd64 focal main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on ubuntu focal
        uses: ./ubuntu/focal
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-jammy:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-ubuntu-jammy-amd64 jammy main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on ubuntu jammy
        uses: ./ubuntu/jammy
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-lunar:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-ubuntu-lunar-amd64 lunar main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on ubuntu lunar
        uses: ./ubuntu/lunar
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-mantic:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-ubuntu-mantic-amd64 mantic main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on ubuntu mantic
        uses: ./ubuntu/mantic
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-noble:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-ubuntu-noble-amd64 noble main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on ubuntu noble
        uses: ./ubuntu/noble
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-oracular:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    strategy:
      fail-fast: false
      matrix:
        stage: [experimental, unstable, testing, release-current] # release-3_0, release-3_1, release-3_2
    env:
      apt-key-url: http://regolith-desktop.org/regolith.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-ubuntu-oracular-amd64 oracular main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ${{ matrix.target-package }} ${{ matrix.stage }} on ubuntu oracular
        uses: ./ubuntu/oracular
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}
