# DO NOT EDIT THIS FILE MANUALLY
#
# This file is auto-generated from .github/scripts/update-workflows and any
# manual changes will be overwritten, nightly.
#
# - to change this file: modify .github/scripts/update-workflows/tmpl/test.tmpl
#
# - to add new distros: modify .github/scripts/update-workflows/main.go and add
#   a new entry to `supportedDistros`
#
# - to add new codename: just simply create a subfolder in corresponding distro
#   folder, and `update-workflows.yml` actions will repopulate this file

name: test

on:
  workflow_dispatch:
  workflow_call:
  push:
    branches:
      - main

jobs:
  debian-bookworm:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    continue-on-error: true
    strategy:
      fail-fast: true
      matrix:
        stage: [unstable, testing, release-3_0, release-3_1]
    env:
      apt-key-url: http://regolith-desktop.org/regolith3.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 ${{ matrix.os.codename }} main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install debian bookworm ${{ matrix.stage }}
        uses: ./debian/bookworm
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  debian-bullseye:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    continue-on-error: true
    strategy:
      fail-fast: true
      matrix:
        stage: [unstable, testing, release-3_0, release-3_1]
    env:
      apt-key-url: http://regolith-desktop.org/regolith3.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 ${{ matrix.os.codename }} main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install debian bullseye ${{ matrix.stage }}
        uses: ./debian/bullseye
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  debian-testing:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    continue-on-error: true
    strategy:
      fail-fast: true
      matrix:
        stage: [unstable, testing, release-3_0, release-3_1]
    env:
      apt-key-url: http://regolith-desktop.org/regolith3.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 ${{ matrix.os.codename }} main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install debian testing ${{ matrix.stage }}
        uses: ./debian/testing
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-focal:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    continue-on-error: true
    strategy:
      fail-fast: true
      matrix:
        stage: [unstable, testing, release-3_0, release-3_1]
    env:
      apt-key-url: http://regolith-desktop.org/regolith3.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 ${{ matrix.os.codename }} main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ubuntu focal ${{ matrix.stage }}
        uses: ./ubuntu/focal
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-jammy:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    continue-on-error: true
    strategy:
      fail-fast: true
      matrix:
        stage: [unstable, testing, release-3_0, release-3_1]
    env:
      apt-key-url: http://regolith-desktop.org/regolith3.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 ${{ matrix.os.codename }} main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ubuntu jammy ${{ matrix.stage }}
        uses: ./ubuntu/jammy
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-mantic:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    continue-on-error: true
    strategy:
      fail-fast: true
      matrix:
        stage: [unstable, testing, release-3_0, release-3_1]
    env:
      apt-key-url: http://regolith-desktop.org/regolith3.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 ${{ matrix.os.codename }} main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ubuntu mantic ${{ matrix.stage }}
        uses: ./ubuntu/mantic
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}

  ubuntu-noble:
    runs-on: ubuntu-latest
    if: "!contains(github.event.head_commit.message, '[ci skip]')"
    continue-on-error: true
    strategy:
      fail-fast: true
      matrix:
        stage: [unstable, testing, release-3_0, release-3_1]
    env:
      apt-key-url: http://regolith-desktop.org/regolith3.key
      apt-repo-line: "deb [arch=amd64] https://regolith-desktop.org/${{ matrix.stage }}-debian-bullseye-amd64 ${{ matrix.os.codename }} main"
      target-package: regolith-desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ubuntu noble ${{ matrix.stage }}
        uses: ./ubuntu/noble
        with:
          apt-key-url: ${{ env.apt-key-url }}
          apt-repo-line: ${{ env.apt-repo-line }}
          target-package: ${{ env.target-package }}
